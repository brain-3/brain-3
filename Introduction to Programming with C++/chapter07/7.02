#include <iostream>
using namespace std;

int main() {

	int numbers[10];

	cout << "Enter 10 numbers: ";

	for (int i = 9; i >= 0; i--) {
		cin >> numbers[i];
	}

	cout << "Show the numbesrs: ";
	for (int j = 0; j < 10; j++) {
		cout << numbers[j] << " ";
	}

	return 0;
}
