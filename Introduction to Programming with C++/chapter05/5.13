#include <iostream>
using namespace std;

int main()
{
    int count = 0;
    for (int i = 100; i <= 200; i++) {
            if ((i % 5 == 0 && i % 6 != 0) || (i % 6 == 0 && i % 5 != 0)) {
                count++;
                if (count % 10 == 0)
                    cout << i << endl;
                else
                    cout << i << " ";
            }
        }

    return 0;
}
