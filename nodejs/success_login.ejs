<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>로그인 페이지</title>
    </head>
    <body>
        <h1>로그인 성공</h1>
        <script>
            let user_id = '<%= user.dataValues.id %>';
            localStorage.setItem('user_id', user_id);
            alert('로그인 성공');
            window.location.href = `http://localhost:3000?user_id=${user_id}`
        </script>
    </body>
</html> 
